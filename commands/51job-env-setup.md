---
description: 一键安装并登录 51job CLI 招聘环境
---

读取并严格执行 `${CLAUDE_PLUGIN_ROOT}/skills/51job-env-setup/SKILL.md` 的流程。先检查 `51job --version`，仅在缺失时运行对应系统的安装脚本；安装或登录过程中不读取、不外传本机 cookie / 凭证。环境就绪后转入 `/recruit-daily-51job`。
