---
name: recruit-daily-51job
description: >
  51job（前程无忧）单通道每日招聘流水线：把「查未读 → 主动寻源 → 硬规则初筛 →
  打招呼 → 补台账 → 出日报」串成一条流水线。当用户说「处理今天的 51job 招聘」、
  「查 51job 有没有未读」、「找 51job 上某岗的人」、「给 51job 合适的人打招呼」、
  「出今天的招聘日报」，或要在 51job 人才池做寻源/初筛/建档动作时使用。
  仅覆盖 51job 通道：BOSS/猎聘走 recruit-daily。
---

# recruit-daily-51job —— 51job 每日招聘流水线

把 51job 一个渠道的「查未读 → 主动寻源 → 初筛 → 打招呼 → 建档 → 出日报」串成一条流水线。

**这是编排文档**：CLI 命令细节与实测坑见 [`references/channels-51job.md`](references/channels-51job.md)，
台账与日报格式见 [`references/ledger-and-report-51job.md`](references/ledger-and-report-51job.md)。
本文档只管「顺序、判断、安全」，不抄数字。

## 铁律：先读事实源，别凭记忆

开工**先读**（都在工作区根下）：
- `CONTEXT.md` —— 术语、**初筛硬规则**、**在招岗位与优先级**（唯一事实源，随时可能更新）。
- `AGENTS.md` —— 分工与「不可逆动作等确认」红线。
- `01-jd/_internal/<role>.md` —— 各岗命脉技能 / 排除信号 / **关键词迭代表**（寻源前必读）。

**本文档不写任何硬规则数字**——年龄线、学历线、地点要求全部以 CONTEXT 为准。
如果 CONTEXT 硬规则区还是空的（没跑过岗位梳理），停下来先走 `skills/recruit-grill/SKILL.md`。

## 前提：51job 已登录

51job 驱动本机真实 Chrome（**默认有头**，窗口弹出是正常现象不是故障）。首次使用先扫码登录：

```bash
51job login          # 打开登录页立即返回
51job wait-login     # 轮询等待扫码/验证码完成（--timeout 秒可调，默认 300）
```

## 主流程（按需跳步）

1. **查未读**：`51job list --unread --json`。
   - **有未读**：逐个按 CONTEXT 硬规则判 → 该回复的回复、该要简历的要、
     该排除的**先记台账等用户确认**（绝不自动点「不合适」）。
   - **没未读**：转主动寻源。

2. **定寻源岗位与优先级**：读 CONTEXT「在招岗位与优先级」，按优先级分配今天的额度。

3. **寻源（51job 两个入口都跑）**，命令与坑见 channels-51job.md：
   - **推荐池**：`51job recommend <岗位> --json`（姓名不打码，带 `forJob` 字段）。
   - **人才搜索**：`51job search "<词>" --job <岗> --json`（姓名打码）。
   - 搜索词用 `_internal/<role>.md` 关键词迭代表里最新一轮的有效词。
   - **完成判据**：每个目标岗，recommend + search 两个入口都跑过——漏一个入口 = 没做完。
     CONTEXT 里若已沉淀「某岗推荐池无效」的结论，可按结论跳过并在日报注明。

4. **硬规则初筛**：严格套 CONTEXT「初筛硬规则」+ 各岗 `_internal` 的**命脉技能**与**排除信号**。
   判定顺序：硬门槛（年龄/学历/地点）→ 命脉对口 → 排除信号 → 评级（⭐~⭐⭐⭐，命脉强 = ⭐⭐⭐）。

5. **打招呼**（对外不可逆——先读下方安全规则）。合适的按 51job 打。
   - **完成判据**：每个过筛人选要么已打招呼、要么记了跳过原因（硬规则/额度/已联系过），逐一回报，无静默遗漏。

6. **补台账** `02-sourcing/dedup-ledger.csv`（去重键 = 姓名+应聘岗位）。格式见 ledger-and-report-51job.md。台账是**唯一事实源**。
   - **完成判据**：本轮接触过的每个候选人在台账里有且仅有一行；无重复序号；按岗位计数与本轮处理人数对得上。

7. **出日报**：
   - 本机有 `lark-cli` 且已配置 → 飞书云文档；
   - 没有 → 写本地 `runtime/reports/YYYY-MM-DD.md`。
   - 两种载体结构相同，见 ledger-and-report-51job.md。内容**从台账同步**，不另立事实源。
   - **完成判据**：日报含今日概况、按岗人选表、待用户确认清单三块，且文档链接/文件路径已交回给用户。

8. **回填关键词迭代表**：从本轮「初筛通过/强匹配」的真实简历反向提取搜索词，
   写回 `01-jd/_internal/<role>.md` 的迭代表（有效词/无效词/积极信号/拒绝模式）。下轮先读它。

## 打招呼安全规则（对外不可逆，最重要）

- **默认对外不可逆动作（打招呼、点「不合适」等）必须经用户确认**。51job `greet` 自带
  Y/N 人机确认（`--no-confirm` 才跳过）；`action unsuitable` 默认二次确认（`--no-confirm` 跳过）。
- 用户在**本轮明确授权**「合适的直接打招呼」即视为本轮持续授权，可直接打，但**逐一回报**打了谁；
  跨轮不自动延续，明天要重新授权。
- **提前看一眼再 Hi**：打招呼前先 `51job greet <姓名> --job <岗> --dry-run`（只出详情摘要不 Hi）
  或用 `51job inspect <姓名> --job <岗> --json` 提取详情，确认对口再打。
- **遇每日沟通额度上限 / 付费弹层**：停下问用户，别自己决定花付费权益。
- **遇风控 / 账号异常 / 验证码**：立即停，不硬闯，报告用户。
- **平台打码名在日报里保持打码**：51job 搜索/推荐是打码名，台账记打码名。
- **商业敏感信息**（CONTEXT 标注敏感）不写进对外话术和日报；对外提及公司/产品名前跟用户核对口径。

## 什么时候不用本流程

- 新开岗位 / 改标准 → `skills/recruit-grill/SKILL.md`。
- 本地收到的简历（猎头/内推/直投）→ `skills/resume-review/SKILL.md`。
- 某岗深度市场盘点 → `skills/market-talent-mapping/SKILL.md`。
- 约面试 / 改期 → `skills/interview-schedule/SKILL.md`（建日程前必经用户确认）。
- BOSS直聘 / 猎聘 → `skills/recruit-daily/SKILL.md`（双通道版）。